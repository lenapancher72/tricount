{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">Inscrivez-vous</h1>

            {{ form_start(registrationForm) }}

            <div class="mb-3 form-group">
                {{ form_label(registrationForm.name, 'Prénom') }}
                {{ form_widget(registrationForm.name, {'attr':{'class':'form-control'}}) }}
            </div>
            <div class="mb-3 form-group">
                {{ form_label(registrationForm.email, 'Mail') }}
                {{ form_widget(registrationForm.email, {'attr':{'class':'form-control'}}) }}
            </div>
            <div class="mb-3 form-group">
                {{ form_label(registrationForm.password, 'Mot de passe') }}
                {{ form_widget(registrationForm.password, {'attr':{'class':'form-control'}}) }}
            </div>

            {{ form_end(registrationForm) }}
            <a class="btn btn-primary" href="{{ path('app_login') }}">S'inscrire</a>
        </div>
    </div>
{% endblock %}
